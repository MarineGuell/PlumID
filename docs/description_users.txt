Rôle		But				Droits							Description
db_admin	Admin complet			ALL PRIVILEGES sur tout					user admin pour l'administration BD
plumid_app	Fonctionnement de l’app		SELECT / INSERT / UPDATE / DELETE			user app pour l'application (backend ou API)
plumid_editor	Gestion manuelle des données	SELECT / INSERT / UPDATE / DELETE (certaines tables)	user pour l'ajout de données (membres du groupe)
plumid_viewer	Lecture seule			SELECT							User…
plumid_ia	Module IA			SELECT (seulement pictures pour le moment)		user IA avec full accès aux tables nécessaires aux modèles


Utilisateur	Peut faire						Ne peut PAS faire
db_admin	TOUT							rien ne doit être refusé
plumid_app	SELECT / INSERT / UPDATE / DELETE sur toutes les tables	pas d’ALTER, pas de DROP, pas de CREATE
plumid_editor	CRUD sur species, pictures, feathers			rien sur users et pas de DROP/ALTER
plumid_viewer	SELECT uniquement					aucune écriture
plumid_ia	SELECT pictures/species					Aucune écriture pour le moment (à voir avec l'ajout de table pour les modèles…)
