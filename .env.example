# --- DB (MySQL recommandé) ---
# Format : mysql+pymysql://USER:PASSWORD@HOST:3306/DBNAME?charset=utf8mb4
DATABASE_URL=mysql+pymysql://plumid:change_me@localhost:3306/plumid?charset=utf8mb4

# Optionnel : SSL
#MYSQL_SSL_CA=/etc/ssl/certs/rds-combined-ca-bundle.pem

# Pool (optionnel)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=mon_user
SMTP_PASSWORD=mon_password
SMTP_FROM=no-reply@plumid.io
FRONTEND_BASE_URL=http://localhost:5173
